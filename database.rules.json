{
  "rules": {
    "userdata": {
      "$uid": {
        ".write": "$uid === auth.uid",
        ".read": "$uid === auth.uid",
        "record": {
          ".indexOn": [
            "project",
            "endTime",
            "startTime"
          ]
        }
      }
    }
  }
}
